# Housemates

An interactive household infographic for an MDES icebreaker.

Open `index.html` in a browser. The page is self-contained, including its images.

`housemates.jpg` is the static version for posting.
